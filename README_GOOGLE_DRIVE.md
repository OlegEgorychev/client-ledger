# Настройка Google Drive API - Инструкция

## Резюме: Что можно автоматизировать, а что нет

### ✅ Я могу сделать автоматически:
1. ✅ Подготовить все команды и скрипты
2. ✅ Создать подробную инструкцию с данными вашего проекта
3. ✅ Настроить код приложения (уже сделано)

### ⚠️ Вы должны сделать вручную (5-10 минут):
1. ⚠️ Получить SHA-1 отпечаток подписи APK
2. ⚠️ Войти в Google Cloud Console
3. ⚠️ Создать проект и настроить OAuth Client ID

**Почему я не могу это сделать?**
- Нужен доступ к вашему Google аккаунту
- Нужен доступ к вашему keystore (файл подписи)
- Это требует интерактивного входа через браузер

---

## Быстрый старт (3 способа получить SHA-1)

### Способ 1: Через Android Studio (САМЫЙ ПРОСТОЙ) ⭐

1. Откройте проект в **Android Studio**
2. В правом нижнем углу нажмите **"Gradle"** (или View → Tool Windows → Gradle)
3. Откройте: **app → Tasks → android → signingReport**
4. Дважды кликните на **"signingReport"**
5. В нижней панели (Run) найдите строку:
   ```
   SHA1: AA:BB:CC:DD:EE:FF:...
   ```
6. **Скопируйте эту строку** (можно с двоеточиями или без)

### Способ 2: Через командную строку (если установлен JDK)

**Windows (PowerShell):**
```powershell
keytool -list -v -keystore "$env:USERPROFILE\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android
```

Найдите строку `SHA1:` в выводе.

**Если keytool не найден:**
- Найдите путь к JDK (обычно `C:\Program Files\Java\jdk-X.X.X\bin\keytool.exe`)
- Или установите JDK: https://www.oracle.com/java/technologies/downloads/

### Способ 3: Через Gradle (в проекте)

Добавьте в `app/build.gradle.kts` перед закрывающей скобкой `android {`:

```kotlin
tasks.register("printSha1") {
    doLast {
        val keystoreFile = file("${System.getProperty("user.home")}/.android/debug.keystore")
        if (keystoreFile.exists()) {
            exec {
                commandLine("keytool", "-list", "-v", "-keystore", keystoreFile.absolutePath, 
                           "-alias", "androiddebugkey", "-storepass", "android", "-keypass", "android")
            }
        } else {
            println("Debug keystore not found at: ${keystoreFile.absolutePath}")
        }
    }
}
```

Затем выполните:
```bash
.\gradlew.bat printSha1
```

---

## Данные вашего проекта (готовы к использованию)

```
Package name: com.clientledger.app
```

**SHA-1 получите одним из способов выше.**

---

## Настройка Google Cloud Console (пошагово)

### Шаг 1: Создайте проект (2 минуты)

1. Откройте https://console.cloud.google.com/
2. Войдите в Google аккаунт
3. Нажмите **"Создать проект"** (Create Project)
4. Название: `Client Ledger Backup`
5. Нажмите **"Создать"**

### Шаг 2: Включите Google Drive API (1 минута)

1. В боковом меню: **"APIs & Services" → "Library"**
2. Найдите **"Google Drive API"**
3. Нажмите **"Enable"** (Включить)

### Шаг 3: Настройте OAuth Consent Screen (2 минуты)

1. В боковом меню: **"APIs & Services" → "OAuth consent screen"**
2. Выберите **"External"** → **"Create"**
3. Заполните:
   - **App name**: `Client Ledger`
   - **User support email**: ваш email
   - **Developer contact email**: ваш email
4. Нажмите **"Save and Continue"** на всех шагах (пропустите опциональные настройки)

### Шаг 4: Создайте OAuth Client ID (2 минуты)

1. В боковом меню: **"APIs & Services" → "Credentials"**
2. Нажмите **"+ CREATE CREDENTIALS" → "OAuth client ID"**
3. Если появится предупреждение — нажмите **"Configure consent screen"** и следуйте шагу 3
4. Выберите **Application type**: **"Android"**
5. Заполните:
   - **Name**: `Client Ledger Android`
   - **Package name**: `com.clientledger.app` ← **скопируйте точно!**
   - **SHA-1 certificate fingerprint**: вставьте SHA-1 из способа 1, 2 или 3
     - Можно с двоеточиями: `AA:BB:CC:DD:EE:FF:...`
     - Или без: `AABBCCDDEEFF...`
     - Оба варианта работают!
6. Нажмите **"Create"**
7. **Готово!** Client ID создан (но вам он не нужен для работы приложения)

---

## Проверка работы (1 минута)

1. Установите APK на устройство
2. Откройте приложение → **Настройки** → **Резервное копирование**
3. Нажмите **"Войти в Google Drive"**
4. Выберите Google аккаунт и разрешите доступ
5. Добавьте или измените клиента/запись
6. Подождите 2 секунды
7. Откройте **Google Drive** → папка **"ClientLedger Backups"**
8. Должен появиться файл `backup_001_YYYY-MM-DD_HH-mm.json` 🎉

---

## Итого: ~10 минут работы

- Получение SHA-1: **2 минуты** (через Android Studio)
- Настройка Google Cloud Console: **5-7 минут**
- Проверка: **1 минута**

**И всё готово!** 🚀

---

## Важно для Release APK

Если будете собирать Release APK:

1. Получите SHA-1 от вашего **release keystore** (тем же способом)
2. Создайте **второй OAuth Client ID** в Google Cloud Console:
   - Package name: `com.clientledger.app` (тот же)
   - SHA-1: от release keystore (другой)
   - Название: `Client Ledger Android Release`

Google Play Services автоматически выберет правильный Client ID.

---

## Устранение проблем

### "DEVELOPER_ERROR" (код 10)
- ✅ Проверьте SHA-1 (должен точно совпадать)
- ✅ Проверьте package name: `com.clientledger.app`

### "SIGN_IN_FAILED"
- ✅ Убедитесь, что Google Drive API включена
- ✅ Проверьте OAuth consent screen

### Кнопка неактивна
- ✅ Google Play Services должны быть установлены
- ✅ Проверьте интернет

---

**Нужна помощь?** Смотрите подробную инструкцию в `GOOGLE_DRIVE_SETUP.md`
