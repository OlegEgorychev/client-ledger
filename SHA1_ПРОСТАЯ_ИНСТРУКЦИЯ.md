# Получить SHA-1 - ПРОСТАЯ ИНСТРУКЦИЯ

## Самый простой способ: Terminal в Android Studio ⭐⭐⭐

### Шаг 1: Откройте Terminal
1. В **Android Studio** внизу найдите вкладку **"Terminal"**
2. Если вкладки нет: **View → Tool Windows → Terminal**

### Шаг 2: Выполните команду
Скопируйте и вставьте эту команду в Terminal (нажмите Enter):

```bash
"%LOCALAPPDATA%\Android\Sdk\jbr\bin\keytool.exe" -list -v -keystore "%USERPROFILE%\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android
```

### Шаг 3: Найдите SHA-1
В выводе найдите строку:
```
SHA1: AA:BB:CC:DD:EE:FF:00:11:22:33:44:55:66:77:88:99:AA:BB:CC:DD
```

**Скопируйте значение:** `AA:BB:CC:DD:EE:FF:00:11:22:33:44:55:66:77:88:99:AA:BB:CC:DD`

---

## Альтернатива: Через батник (двойной клик)

1. В папке проекта `D:\Programs\test` найдите файл **`get-sha1.bat`**
2. **Дважды кликните** на `get-sha1.bat`
3. Окно командной строки покажет SHA-1

---

## Альтернатива: Через Gradle (если сработает)

1. В Android Studio нажмите **File → Sync Project with Gradle Files** (🔄 сверху)
2. Подождите синхронизации
3. Справа **Gradle** → **app** → **Tasks** → **other** → **printSha1**
4. Дважды кликните на `printSha1`
5. Внизу в панели Build увидите SHA-1

---

## Что делать с SHA-1?

После получения SHA-1 у вас будут данные:

```
Package name: com.clientledger.app
SHA-1 fingerprint: [ваше значение SHA-1]
```

Эти данные нужны для создания OAuth Client ID в Google Cloud Console.

---

## Если ничего не работает

**Создайте OAuth Client ID БЕЗ SHA-1:**
1. В Google Cloud Console создайте OAuth Client ID
2. Package name: `com.clientledger.app`
3. **SHA-1 можно оставить пустым** для тестирования
4. Попробуйте войти в приложении
5. Если получите ошибку `DEVELOPER_ERROR (10)`, Google покажет нужный SHA-1 в сообщении об ошибке
6. Обновите OAuth Client ID с правильным SHA-1

---

**Рекомендую начать с Terminal в Android Studio - это самый надежный способ!** 🚀
