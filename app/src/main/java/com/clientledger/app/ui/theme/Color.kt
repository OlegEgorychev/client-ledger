package com.clientledger.app.ui.theme

import androidx.compose.ui.graphics.Color

// Brand Colors - Deep Indigo/Blue gradient palette
val DeepBlue = Color(0xFF1A237E)      // Deep blue (primary base)
val Indigo = Color(0xFF3949AB)        // Indigo (primary)
val LightIndigo = Color(0xFF5C6BC0)   // Light indigo (primary variant)

// Secondary - Cool Blue/Cyan
val CoolBlue = Color(0xFF0277BD)      // Cool blue
val Cyan = Color(0xFF00ACC1)         // Cyan accent
val LightCyan = Color(0xFF4DD0E1)    // Light cyan (teal)

// Tertiary - Soft Green (growth, positive)
val SoftGreen = Color(0xFF4CAF50)    // Soft green
val LightGreen = Color(0xFF81C784)   // Light green variant

// Error - Muted Red
val MutedRed = Color(0xFFD32F2F)     // Muted red for cancellations
val LightRed = Color(0xFFE57373)     // Light red variant

// Background - Dark theme (near-black blue)
val DarkBackground = Color(0xFF0A0E27)  // Near-black blue
val DarkSurface = Color(0xFF121629)     // Dark surface
val DarkSurfaceVariant = Color(0xFF1A1F3A) // Dark surface variant

// Background - Light theme (2025 style: high contrast, modern)
val LightBackground = Color(0xFFF8F9FA)  // Near-white with slight tint for contrast
val LightSurface = Color(0xFFFFFFFF)      // Pure white for cards (maximum contrast)
val LightSurfaceVariant = Color(0xFFF1F3F5) // Slightly darker gray for better separation
val LightOutline = Color(0xFFCED4DA)     // More visible outline (higher contrast)
val LightOutlineVariant = Color(0xFFDEE2E6) // More visible outline variant

// Legacy colors (kept for compatibility, can be removed later)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
