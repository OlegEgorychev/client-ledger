# Как запустить батник, чтобы увидеть ошибку

## Способ 1: Через командную строку (чтобы увидеть ошибку)

1. Нажмите **Win + R**
2. Введите `cmd` и нажмите **Enter**
3. В командной строке выполните:

```cmd
cd D:\Programs\test
get-sha1-simple.bat
```

Теперь вы увидите все сообщения и ошибки!

---

## Способ 2: Через Android Studio Terminal

1. В Android Studio откройте **Terminal** (вкладка внизу)
2. Выполните:

```cmd
cd D:\Programs\test
get-sha1-simple.bat
```

---

## Способ 3: Самый простой - через Android Studio напрямую

### Вариант A: Terminal в Android Studio (CMD)

1. В Android Studio откройте **Terminal**
2. Введите `cmd` и нажмите Enter (переключитесь на CMD)
3. Выполните команду напрямую:

```cmd
"%LOCALAPPDATA%\Android\Sdk\jbr\bin\keytool.exe" -list -v -keystore "%USERPROFILE%\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android
```

4. Найдите строку **`SHA1:`** и скопируйте значение

---

## Способ 4: Если ничего не работает - обходной путь через Google

**Это самый надежный способ!** Google сам покажет нужный SHA-1:

1. **Создайте OAuth Client ID БЕЗ SHA-1:**
   - Откройте https://console.cloud.google.com/
   - APIs & Services → Credentials → Create Credentials → OAuth client ID
   - Application type: **Android**
   - Package name: `com.clientledger.app`
   - **SHA-1 оставьте пустым** (или введите `00:00:00:00:00:00:00:00:00:00:00:00:00:00:00:00:00:00:00:00` для теста)
   - Нажмите Create

2. **Попробуйте войти в приложении:**
   - Установите APK на устройство
   - Откройте приложение → Настройки → Резервное копирование
   - Нажмите "Войти в Google Drive"

3. **Если появится ошибка:**
   - Откройте **Logcat** в Android Studio (вкладка внизу)
   - Фильтр: `GoogleSignIn` или `DEVELOPER_ERROR`
   - Найдите сообщение об ошибке
   - В ошибке будет указан **SHA-1**, который нужен

4. **Обновите OAuth Client ID** с правильным SHA-1

---

## Способ 5: Через уже собранный APK

Если у вас уже есть собранный APK:

1. Найдите APK в: `D:\Programs\test\app\build\outputs\apk\debug\app-debug.apk`
2. В командной строке выполните:

```cmd
"%LOCALAPPDATA%\Android\Sdk\jbr\bin\keytool.exe" -list -printcert -jarfile "D:\Programs\test\app\build\outputs\apk\debug\app-debug.apk"
```

3. Найдите строку **`SHA1:`**

---

## Рекомендация

**Используйте Способ 4 (обходной путь через Google)** - это самый надежный способ, который точно сработает! 🚀

Google сам покажет нужный SHA-1 при попытке входа, даже если вы не знаете его заранее.
