# Как получить SHA-1 - Инструкция (исправлено)

## Способ 1: Используйте батник get-sha1-debug.bat ⭐ (САМЫЙ ПРОСТОЙ)

Я создал улучшенную версию батника, которая показывает все ошибки:

1. Откройте **Проводник Windows**
2. Перейдите в папку: `D:\Programs\test`
3. Найдите файл **`get-sha1-debug.bat`** (новая версия с отладкой)
4. **Дважды кликните** на него
5. Окно **НЕ закроется автоматически** - вы увидите результат

**Если окно все равно закрывается быстро:**
- Запустите через командную строку:
  1. Нажмите `Win + R`
  2. Введите `cmd` и нажмите Enter
  3. Перейдите в папку проекта: `cd D:\Programs\test`
  4. Выполните: `get-sha1-debug.bat`

---

## Способ 2: Через Android Studio Terminal (PowerShell)

1. В Android Studio откройте **Terminal** (вкладка внизу)
2. Переключитесь на CMD: введите `cmd` и нажмите Enter
3. Выполните команду:

```cmd
"%LOCALAPPDATA%\Android\Sdk\jbr\bin\keytool.exe" -list -v -keystore "%USERPROFILE%\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android
```

4. Найдите строку **`SHA1:`** в выводе
5. Скопируйте значение (например: `AA:BB:CC:DD:EE:FF:...`)

---

## Способ 3: Если батник не находит keytool

### Вариант A: Найти keytool вручную

1. Откройте Проводник
2. Перейдите в: `C:\Users\egory\AppData\Local\Android\Sdk\jbr\bin\`
3. Проверьте, есть ли там файл `keytool.exe`

**Если файл есть:**
- Используйте полный путь в команде:

```cmd
"C:\Users\egory\AppData\Local\Android\Sdk\jbr\bin\keytool.exe" -list -v -keystore "%USERPROFILE%\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android
```

**Если файла нет:**
- Запустите Android Studio хотя бы один раз
- Или установите Java JDK

---

## Способ 4: Получить SHA-1 через ошибку Google (обходной путь)

Если не получается получить SHA-1 локально:

1. **Создайте OAuth Client ID БЕЗ SHA-1:**
   - Откройте Google Cloud Console
   - Создайте OAuth Client ID (Android)
   - Package name: `com.clientledger.app`
   - **SHA-1 оставьте пустым** (или введите любые символы для теста)

2. **Попробуйте войти в приложении:**
   - Откройте приложение → Настройки → Войти в Google Drive

3. **Если появится ошибка `DEVELOPER_ERROR (10)`:**
   - Откройте **Logcat** в Android Studio (вкладка внизу)
   - Найдите сообщение об ошибке
   - В ошибке будет указан **правильный SHA-1**, который нужен
   - Или Google покажет его в сообщении об ошибке

4. **Обновите OAuth Client ID** с правильным SHA-1

---

## Способ 5: Если у вас уже собран APK

1. Соберите APK в Android Studio: **Build → Build Bundle(s) / APK(s) → Build APK(s)**
2. После сборки нажмите **"locate"** в уведомлении
3. APK будет в: `app\build\outputs\apk\debug\app-debug.apk`
4. В командной строке выполните:

```cmd
"%LOCALAPPDATA%\Android\Sdk\jbr\bin\keytool.exe" -list -printcert -jarfile "D:\Programs\test\app\build\outputs\apk\debug\app-debug.apk"
```

5. Найдите строку **`SHA1:`**

---

## Что делать если ничего не работает?

**Просто создайте OAuth Client ID без SHA-1** (способ 4 выше) - Google сам покажет нужный SHA-1 при попытке входа! Это самый надежный способ. 🚀

---

## Данные для Google Cloud Console (после получения SHA-1)

```
Package name: com.clientledger.app
SHA-1 fingerprint: [ваш SHA-1 из любого способа выше]
```

Скопируйте эти данные для создания OAuth Client ID в Google Cloud Console.
